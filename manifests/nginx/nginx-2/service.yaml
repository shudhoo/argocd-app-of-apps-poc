apiVersion: v1
kind: Service
metadata:
  name: apache-1-service
  namespace: nginx
spec:
  type: NodePort
  selector:
    app: nginx-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30011