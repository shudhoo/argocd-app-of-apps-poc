apiVersion: v1
kind: Service
metadata:
  name: erp-web-app-1-service
  namespace: erp
spec:
  type: NodePort
  selector:
    app: erp-web-app-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30011